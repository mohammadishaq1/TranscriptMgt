@model IEnumerable<DataAccess.StudentPromoteTable>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IsActive)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProgrammeSemestersTable.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StudentTable.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IsActive)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProgrammeSemestersTable.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StudentTable.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StudentPromoteID }) |
            @Html.ActionLink("Details", "Details", new { id=item.StudentPromoteID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StudentPromoteID })
        </td>
    </tr>
}

</table>
